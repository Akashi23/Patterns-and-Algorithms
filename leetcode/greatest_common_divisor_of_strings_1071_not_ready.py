class Solution:
    def gcdOfStrings(self, str1: str, str2: str) -> str:
        str_common = ''
        while str_common
        for i, v in zip(str1, str2):
            if i == v:
                str_common += v
        return str_common


solution = Solution()
answer = solution.gcdOfStrings("ABCABC", "ABCABCABC")
print(answer)
